<h1>Search Results</h1>

<% @search_results.each do |photo| %>
  <p>
    <strong><%= photo.title %></strong>, by: <%= photo.artist %>
  </p>
  <%= link_to image_tag(photo.url), "/photos/#{photo.id}"%>
<% end %>
